<body>
    <h1 id="result"></h1>  
    <script>
    let array1 = [3,7,9,11,13,15]
    let array2 = [8,7,6,5,4]
    let output = document.getElementById("result")
    let array = array1.splice(0, 1, 27) (starting index, no of elements to delete, elements to be added)
    array1.forEach(cubic) (CUBIC IS A CALLBACK FUNCTION)
    function cubic(element, index, array) {
        array[index]=element*element*element
        output.innerHTML = array
    }
